# WireGuard Split Tunneling - Раздельное туннелирование

## 📋 Описание проекта

Проект предназначен для настройки **раздельного туннелирования (split tunneling)** в WireGuard на Windows. Это позволяет направлять трафик определенных доменов через локальную сеть (в обход VPN), в то время как остальной трафик идет через VPN-туннель.

### Зачем это нужно?

- **Российские сервисы** (Госуслуги, банки, госучреждения) часто блокируют доступ через VPN
- **Локальные ресурсы** могут быть недоступны при активном VPN-подключении
- **Оптимизация скорости** - локальный трафик не проходит через удаленные серверы
- **Избежание блокировок** - некоторые сайты определяют и блокируют VPN-трафик

---

## 🗂️ Структура проекта

```
Wireguard-split-tunneling/
├── add_routes.ps1                    # Скрипт добавления маршрутов (домены)
├── remove_routes.ps1                 # Скрипт удаления маршрутов (старый)
├── remove_routes_all.ps1             # Скрипт удаления всех маршрутов через шлюз
├── check_domains.ps1                 # Скрипт проверки доступности доменов
├── domains.conf                      # Основной список доменов (22 домена)
├── domains_ru.conf                   # Расширенный список российских доменов (350+)
├── wireguard-mobile-exclusions.md    # Инструкция для мобильных устройств
└── image/                            # Изображения для документации
```

### Новые файлы (Июнь 2026):

- **`remove_routes_all.ps1`** - удаляет все маршруты через шлюз (не зависит от DNS)
- **`check_domains.ps1`** - проверяет доступность доменов и комментирует недоступные
- **`domains_ru.conf`** - расширенный список из 350+ российских доменов с поддоменами

---

## ⚙️ Системные требования

- **Операционная система**: Windows 10/11
- **PowerShell**: версия 5.1 или выше
- **Права администратора**: обязательны для изменения маршрутов
- **WireGuard**: установленный клиент (опционально, для полной настройки)

---

## 🚀 Быстрый старт

### Шаг 1: Выбор списка доменов

**Вариант А: Минимальный список (`domains.conf`)**
- 22 домена основных сервисов
- Подходит для базового использования

**Вариант Б: Расширенный список (`domains_ru.conf`)**
- 350+ доменов российских сервисов
- Включает банки, маркетплейсы, стриминги, госуслуги
- Автоматически проверен на доступность

### Шаг 2: Настройка конфигурации

Откройте выбранный файл (`domains.conf` или `domains_ru.conf`) и отредактируйте при необходимости:

```conf
# Один домен на строку!
# Комментарии начинаются с #

# Госуслуги
gosuslugi.ru
esia.gosuslugi.ru
gu-st.ru
sfd.gosuslugi.ru
lk.gosuslugi.ru

# Яндекс (геопозиция)
mc.yandex.ru

# Маркетплейсы
ozon.ru
avito.ru
```

### Шаг 3: Проверка доменов (только для `domains_ru.conf`)

Если используете расширенный список, проверьте актуальность доменов:

```powershell
# Правой кнопкой мыши на PowerShell → "Запуск от имени администратора"
cd C:\Path\To\Wireguard-split-tunneling
.\check_domains.ps1
```

Скрипт:
- Проверяет каждый домен через DNS-резолвинг
- Комментирует недоступные домены: `# НЕДОСТУПЕН: domain.ru`
- Создаёт резервную копию `domains_ru.conf.backup`

### Шаг 4: Изменение шлюза по умолчанию

В файлах скриптов измените переменную `$Gateway` на IP-адрес вашего **локального шлюза**:

```powershell
$Gateway = "192.168.1.1"  # Замените на ваш шлюз
```

**Как узнать свой шлюз:**
1. Откройте командную строку (cmd)
2. Выполните команду: `ipconfig`
3. Найдите строку **"Основной шлюз"** в вашем активном подключении

### Шаг 5: Запуск скрипта добавления маршрутов

**ВАЖНО:** Запускайте PowerShell **от имени администратора**!

#### Обычный запуск (маршруты удаляются после перезагрузки):

```powershell
.\add_routes.ps1
```

#### С постоянными маршрутами (сохраняются после перезагрузки):

```powershell
.\add_routes.ps1 -p
```

**Параметр `-p`:** делает маршруты постоянными - они сохраняются после перезагрузки системы.

---

## 📖 Описание скриптов

### add_routes.ps1

**Назначение:** Добавляет статические маршруты для доменов из `domains.conf`.

**Как работает:**
1. Читает список доменов из `domains.conf`
2. Для каждого домена:
   - Разрешает доменное имя в IP-адреса (IPv4)
   - Удаляет существующий маршрут (если есть)
   - Добавляет новый маршрут с метрикой 1 через локальный шлюз
3. Выводит результат операции

**Параметры:**
- `-p` - создание постоянных маршрутов (сохраняются после перезагрузки)

---

### remove_routes_all.ps1 (НОВЫЙ)

**Назначение:** Удаляет ВСЕ маршруты через указанный шлюз, независимо от DNS.

**Преимущества перед `remove_routes.ps1`:**
- Не зависит от текущих DNS-записей
- Удаляет маршруты даже если IP домена изменился
- Работает быстрее (нет DNS-запросов)

**Параметры:**
- Без параметра: удаляет временные маршруты
- `-p`: удаляет постоянные маршруты

**Пример использования:**
```powershell
.\remove_routes_all.ps1      # Удалить временные маршруты
.\remove_routes_all.ps1 -p   # Удалить постоянные маршруты
```

---

### check_domains.ps1 (НОВЫЙ)

**Назначение:** Проверяет доступность доменов и комментирует недоступные.

**Как работает:**
1. Читает `domains_ru.conf`
2. Для каждого домена выполняет DNS-резолвинг
3. Если домен недоступен - комментирует строку: `# НЕДОСТУПЕН: domain.ru`
4. Создаёт резервную копию оригинального файла

**Когда использовать:**
- Перед первым использованием `domains_ru.conf`
- Периодическая проверка актуальности списка
- После массовых изменений в списке доменов

---

## 🔧 Возможные проблемы и их решение

### Проблема: "Отказано в доступе"

**Причина:** Скрипт запущен без прав администратора.

**Решение:**
1. Закройте текущее окно PowerShell
2. Нажмите `Win + X`
3. Выберите "Windows PowerShell (администратор)"
4. Запустите скрипт снова

### Проблема: "Не удалось найти IP для domain.ru"

**Причины:**
- Домен не существует или опечатка
- Проблемы с DNS
- Домен временно недоступен

**Решения:**
1. Проверьте правильность написания домена
2. Попробуйте выполнить `nslookup domain.ru` в командной строке
3. Используйте `check_domains.ps1` для массовой проверки
4. Проверьте настройки DNS в свойствах сетевого подключения

### Проблема: Маршруты не работают после добавления

**Причины:**
- Неверный IP шлюза
- Конфликт с другими маршрутами
- WireGuard перезаписывает маршруты

**Решения:**
1. Проверьте правильность `$Gateway` в скриптах
2. Выполните `route print` и проверьте наличие маршрутов
3. Запускайте `add_routes.ps1` **после** подключения к WireGuard
4. **ВАЖНО:** В настройках туннеля WireGuard **ОТКЛЮЧИТЕ** опцию "Block untunneled traffic (kill-switch)"

### Проблема: После перезагрузки маршруты исчезли

**Решение:** Используйте параметр `-p` или настройте автозагрузку (см. раздел ниже)

---

## ⚙️ Настройка WireGuard клиента

### Отключение Kill-Switch (ОБЯЗАТЕЛЬНО!)

**ВАЖНО:** Для работы split tunneling необходимо отключить опцию kill-switch в WireGuard!

1. Откройте клиент WireGuard
2. Нажмите кнопку **Edit** рядом с вашим туннелем
3. **СНИМИТЕ галочку** с опции **"Block untunneled traffic (kill-switch)"**
4. Нажмите **Save**

**Почему это важно:**
- WireGuard по умолчанию блокирует весь трафик, который не идет через VPN
- Это предотвращает работу локальных маршрутов
- Без отключения этой опции ваши исключения доменов НЕ БУДУТ работать

### Порядок действий:

1. **Отключите kill-switch** в настройках туннеля WireGuard
2. Подключитесь к VPN
3. Запустите `add_routes.ps1` от имени администратора
4. Проверьте работу исключенных доменов

---

## 🔄 Автоматический запуск при старте системы

### Способ 1: Использовать параметр -p (простой)

Запустите скрипт с параметром `-p` один раз - маршруты сохранятся навсегда:

```powershell
.\add_routes.ps1 -p
```

**Плюсы:**
- Не нужно настраивать автозагрузку
- Маршруты сохраняются после перезагрузки

**Минусы:**
- При изменении `domains.conf` нужно пересоздавать все маршруты

---

### Способ 2: Автозагрузка скрипта (гибкий)

1. Нажмите `Win + R`, введите `shell:startup`
2. Создайте ярлык для `add_routes.ps1` в открывшейся папке
3. В свойствах ярлыка → Дополнительно → Запуск от имени администратора

**Плюсы:**
- Маршруты всегда актуальные (IP разрешаются заново при каждом запуске)
- Легко обновлять список доменов

---

### Способ 3: Планировщик заданий (для продвинутых)

```powershell
# Выполнить от имени администратора
$action = New-ScheduledTaskAction -Execute "PowerShell.exe" -Argument "-ExecutionPolicy Bypass -File `"C:\Path\To\add_routes.ps1`""
$trigger = New-ScheduledTaskTrigger -AtLogOn
$principal = New-ScheduledTaskPrincipal -UserId "SYSTEM" -LogonType ServiceAccount -RunLevel Highest
Register-ScheduledTask -TaskName "WireGuard Routes" -Action $action -Trigger $trigger -Principal $principal
```

---

## 📱 Настройка для мобильных устройств

Для Android и iOS устройств используйте встроенную функцию исключений приложений в официальном клиенте WireGuard.

Подробная инструкция с скриншотами находится в файле [wireguard-mobile-exclusions.md](wireguard-mobile-exclusions.md).

**Краткая инструкция:**
1. Откройте WireGuard на устройстве
2. Нажмите на название туннеля
3. Нажмите иконку карандаша ✏️
4. В разделе "Интерфейс" найдите "Все приложения"
5. Выберите режим:
   - **"Исключить для"** - выбранные приложения работают в обход VPN
   - **"Включить для"** - только выбранные приложения работают через VPN
6. Отметьте нужные приложения
7. Сохраните настройки (иконка дискеты 💾)

---

## 🎯 Примеры использования

### Сценарий 1: Работа с российскими банками через VPN

```conf
# Банки и финансовые сервисы
sberbank.ru
online.sberbank.ru
tinkoff.ru
alfabank.ru
vtb.ru
```

### Сценарий 2: Доступ к государственным сервисам

```conf
# Госуслуги и налоговая
gosuslugi.ru
esia.gosuslugi.ru
nalog.ru
lkul.nalog.ru
```

### Сценарий 3: Оптимизация для стриминга и загрузки

```conf
# Российские CDN и стриминговые сервисы
yandex.ru
kinopoisk.ru
ivi.ru
more.tv
start.ru
```

---

## ⚠️ Важные предупреждения

1. **Безопасность:** Трафик, идущий в обход VPN, не шифруется и виден вашему провайдеру
2. **DNS-утечки:** Убедитесь, что DNS-запросы также идут через нужный интерфейс
3. **Конфликты:** Не добавляйте слишком много маршрутов - это может замедлить работу сети
4. **Обновление IP:** Если сайт меняет IP-адрес, нужно перезапустить `add_routes.ps1`
5. **IPv6:** Скрипты работают только с IPv4. Для IPv6 нужна отдельная настройка
6. **Количество маршрутов:** `domains_ru.conf` содержит 350+ доменов, что создаст столько же маршрутов. Windows поддерживает тысячи маршрутов, но это может повлиять на производительность.

---

## 🔄 Обновление списка доменов

### Если вы ДОБАВЛЯЕТЕ домены:

Просто добавьте их в файл конфигурации и запустите:
```powershell
.\add_routes.ps1
```
Скрипт сам удалит старые маршруты и добавит новые.

### Если вы УДАЛЯЕТЕ домены:

**⚠️ ВАЖНО:** Сначала очистите маршруты, ПОТОМ изменяйте файл!

```powershell
# 1. Запустите удаление всех маршрутов через шлюз
.\remove_routes_all.ps1

# 2. Теперь измените файл конфигурации (удалите ненужные домены)

# 3. Добавьте актуальные маршруты
.\add_routes.ps1
```

**Почему такой порядок?** `remove_routes_all.ps1` удаляет все маршруты через шлюз, независимо от содержимого файла конфигурации.

---

## 🛠️ Расширенная настройка

### Изменение метрики маршрута

Метрика определяет приоритет маршрута. Чем меньше число, тем выше приоритет.

В `add_routes.ps1` измените параметр `metric`:
```powershell
route.exe add $ipStr mask $Mask $Gateway metric 1 if $IfIndex
#                                          ^^^^^^^^
#                                          Измените это значение
```

### Проверка DNS

Убедитесь, что DNS-запросы идут через правильный интерфейс:
```powershell
ipconfig /all
# Проверьте "DNS-серверы" для каждого адаптера
```

### Просмотр таблицы маршрутизации

```powershell
route print | findstr "192.168.3.1"
```

---

## 📞 Поддержка и обратная связь

Если возникли проблемы:

1. Проверьте логи выполнения скриптов
2. Выполните `route print` для просмотра таблицы маршрутизации
3. Используйте `tracert domain.ru` для проверки пути до сайта
4. Проверьте настройки WireGuard (метрики, DNS)
5. Запустите `check_domains.ps1` для проверки доступности доменов

---

## 📄 Лицензия

Этот проект предоставляется "как есть" без каких-либо гарантий. Используйте на свой страх и риск.

---

## 🔗 Полезные ссылки

- [Официальный сайт WireGuard](https://www.wireguard.com/)
- [Документация WireGuard](https://www.wireguard.com/quickstart/)
- [Split Tunneling объяснение](https://en.wikipedia.org/wiki/Split_tunneling)
- [Команда route.exe документация](https://docs.microsoft.com/en-us/windows-server/administration/windows-commands/route)

---

**Версия:** 2.0  
**Последнее обновление:** 14 июня 2026  
**Новые возможности:**
- `remove_routes_all.ps1` - универсальное удаление маршрутов
- `check_domains.ps1` - проверка доступности доменов
- `domains_ru.conf` - расширенный список из 350+ российских доменов
- Поддержка параметра `-p` для постоянных маршрутов
